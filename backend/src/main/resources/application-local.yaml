spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lms_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: lms_user
    password: "1234"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update   # 로컬 개발 전용 — 프로덕션에서는 validate 사용
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: dGVzdFNlY3JldEtleUZvckpXVEF1dGhlbnRpY2F0aW9uMTIzNDU2Nzg=
  expiration-ms: 86400000

ai:
  server:
    url: http://localhost:5000

cors:
  allowed-origins: http://localhost:3000,http://localhost:3005,http://127.0.0.1:3000,http://127.0.0.1:3005,http://192.168.2.46:3005

logging:
  level:
    com.hr.backend.security: INFO
    org.springframework.security: DEBUG
